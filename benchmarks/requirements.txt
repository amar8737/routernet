# Standalone OpenML benchmark for routernet
#
# These are the benchmark-only dependencies. They are intentionally NOT part of
# the routernet package metadata, so installing routernet from PyPI never pulls
# them in.
#
#   pip install -r requirements.txt

openml>=0.14
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
tabulate>=0.9

# Optional: include if you want XGBoost as an additional gate baseline
xgboost>=1.7
